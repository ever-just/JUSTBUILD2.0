{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "export NODE_OPTIONS='--max-old-space-size=8192' && yarn install --immutable --network-timeout 600000 && yarn workspace @open-swe/shared build && yarn workspace @open-swe/agent build",
    "watchPatterns": [
      "apps/open-swe/**",
      "packages/shared/**"
    ]
  },
  "deploy": {
    "startCommand": "yarn workspace @open-swe/agent dev",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckPath": "/api/agent/ok",
    "healthcheckTimeout": 30,
    "numReplicas": 1
  },
  "environments": {
    "production": {
      "build": {
        "buildCommand": "export NODE_OPTIONS='--max-old-space-size=8192' && yarn install --immutable --network-timeout 600000 && yarn workspace @open-swe/shared build && yarn workspace @open-swe/agent build"
      }
    }
  }
}
